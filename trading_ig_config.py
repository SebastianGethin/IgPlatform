'''
This is a template for the trading_ig_config file which holds authentication credentials. It is required for the API handler to function.
The first step in using the files in this repository is to download this template, populate the fields with your credentials (available
from ig.com) and save it to your project folder.
'''

class api_config(object):
    username = ''
    password = ''
    api_key = ''
    acc_type = ''
    acc_number = ''
